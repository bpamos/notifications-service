# notifications-service

Worker that consumes a queue stub and sends notifications. Each job envelope carries a JWT that the worker verifies before delivery.

This repository is a **demo fixture** for the Fleet CVE Remediation exercise. It is not a production service.
